spring:
  cloud:
    nacos:
      config:
        server-addr: nacos:8848
        file-extension: yml
      discovery:
        server-addr: nacos:8848

    gateway:
      routes:
#        - id: auth-service
#          uri: lb://auth-service
#          predicates:
#            - Path=/uaa/**
#          filters:
#            - StripPrefix=1